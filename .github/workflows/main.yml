name: rebuild AIDE
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get apktool
        run: |
          wget -q https://github.com/iBotPeaches/Apktool/releases/download/v2.7.0/apktool_2.7.0.jar
          find
      - name: Get APK
        run: |
          wget -q -O app.apk https://secure.downloadfp.com/android/US/com.aide.ui/2008210017/aide.apk?st=wfS_JvA8mDhSb7Mf2IHOFA&e=1675735636
          find -type f
      - name: Decompress resources
        run: java -jar apktool_2.7.0.jar d -s app.apk
      - name: Edit sources
        run: echo TO DO
      - name: Build APK
        run: java -jar apktool_2.7.0.jar -b app
      - name: Display result
        run: find -type f -name "*.apk"
